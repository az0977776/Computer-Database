extends layout

block content
  div.container
    h1 Computers
    ul
      - each val in rooms
        - var str = "/room/" + val
        - var btn = "Room " + val
        li
          a(href=str)
            button.wavs-effect.waves-light.btn #{btn}
          br
          br
        